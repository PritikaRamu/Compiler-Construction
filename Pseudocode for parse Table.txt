Pseudocode for parse Table{

    // Case 1
    for each(A-> alpha){ //alpha = RHS
        for each (t in First(alpha)){
            Table[A, t] = alpha
        }
    }

    // Case 2
    if( epsilon is in First(alpha)){
        for each(b in Follow(A)){ // Including SENTINEL
            Table[A, b] = alpha
        }
    }

}


Pseudocode for First(alpha){
    A -> BCDF

    // Case 1 : b is a Terminal
    First(bCDF) = b

    // Case 2 : B is a NT
    First(A) = {First(B)-epsilon} U {First(C)-epsilon} U ...
}

Pseudocode for epsilonCheck(alpha){

}